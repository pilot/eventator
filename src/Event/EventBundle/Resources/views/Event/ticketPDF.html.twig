<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>TicketPDF</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">
    <style>
        body {
            width: 900px;
        }
        .ticket-container{
            border: 1px solid black;
            color: black;
            background-size: 108%;
            width: 100%;
            font: 26px PermianSansTypeface;
        }
        .ticket-container:after {
            content: "";
            display: table;
            clear: both;
        }
        .event_logo {
            width: 75%;
            margin-left: 50px;
            margin-top: 50px;
        }
        .left_side{
            width: 60%;
            float: left;
        }
        .right_side{
            width: 40%;
            text-align: right;
            float: left;
            padding-top: 52px;
        }
        .ticket_qr_container img {
            width: 60%;
            margin-right: 50px;
        }
        .eventator_logo_container img{
            width: 60%;
            margin-bottom: 25px;
            margin-right: 50px;
        }
        .name_container, .venue_container {
            margin-left: 50px;
        }
        .name_container {
            font: 38pt PT sans-serif;
            margin-top: 50px;
            margin-bottom: 30px;
            font-weight: bold;
        }
        .venue_container {
            margin-bottom: 25px;
        }
        .name_container p, .venue_container p{
            margin: 5px;
        }
        .venue_container img {
            width: 20px;
            margin-right: 35px;
        }
        span.social_link {
            padding-left: 25px;
            position: relative;
        }
        span.social_link:before {
            display: inline-block;
            content: '';
            width: 20px;
            height: 20px;
            background-size: contain !important;
            position: absolute;
            left: -25px;
            top: 5px;
        }
        span.social_fb:before {
            background: url({{ app.request.getSchemeAndHttpHost() ~ asset('bundles/eventevent/img/fb-pdf.png') }}) no-repeat center;
        }
        span.social_twt:before {
            background: url({{ app.request.getSchemeAndHttpHost() ~ asset('bundles/eventevent/img/twitter-pdf.png') }}) no-repeat center;
        }
    </style>
</head>
<body>
<div class="ticket-container">
    <div class="left_side">
        <img class="event_logo" src="{{ app.request.getSchemeAndHttpHost() ~ asset('bundles/eventevent/img/rn_logo.png') }}" alt />
        <div class="name_container">
            <p class="guest_firstname">{{ soldTicket.firstName }}</p>
            <p class="guest_lastname">{{ soldTicket.lastName }}</p>
        </div>
        <div class="venue_container">
            {% if soldTicket.ticket.event.facebook %}
            <p class="social">
                <span class="social_link social_fb">{{ soldTicket.ticket.event.facebook }}</span>
            </p>
            {% endif %}
            {% if soldTicket.ticket.event.twitter %}
            <p class="social">
                <span class="social_link social_twt">{{ soldTicket.ticket.event.twitter }}</span>
            </p>
            {% endif %}
        </div>
    </div>
    <div class="right_side">
        <div class="eventator_logo_container">
            <img src="{{ app.request.getSchemeAndHttpHost() ~ asset('bundles/eventevent/img/eventator-logo-pdf.png') }}" alt />
        </div>
        <div class="ticket_qr_container">
            <img src="{{ soldTicket.QRCode }}" alt />
        </div>
    </div>
</div>
</body>
</html>
